server:
  port: 8093
  error:
    include-message: always
    include-binding-errors: always
spring:
  application:
    name: customerservice
  datasource:
    url: jdbc:h2:mem:account;DB_CLOSE_DELAY=-1;DB_CLOSE_ON,EXIT=FALSE
    driverClassName: org.h2.Driver
