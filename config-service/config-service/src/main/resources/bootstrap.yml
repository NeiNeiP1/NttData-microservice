server:
  port: 8084

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NeiNeiP1/NttData-microservice.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: pass

